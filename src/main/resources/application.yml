spring:
  application:
    name: wmc-guiaremision

  datasource:
    url: jdbc:mysql://localhost:3306/eteamfact
    username: gre
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

# Configuración SUNAT
sunat:
  api:
    base-url: https://api-cpe.sunat.gob.pe
    token-url: https://api-seguridad.sunat.gob.pe/v1/clientessol/{client_id}/oauth2/token
    send-url: https://api-cpe.sunat.gob.pe/v1/contribuyente/gem/comprobantes/{filename}
    ticket-url: https://api-cpe.sunat.gob.pe/v1/contribuyente/gem/comprobantes/envios/{numTicket}
    beta:
      base-url: https://gre-test.nubefact.com
      token-url: https://gre-test.nubefact.com/v1/clientessol/{client_id}/oauth2/token
      send-url: https://gre-test.nubefact.com/v1/contribuyente/gem/comprobantes/{numRucEmisor}-{codCpe}-{numSerie}-{numCpe}
      ticket-url: https://gre-test.nubefact.com/v1/contribuyente/gem/comprobantes/envios/{numTicket}
      client-id: test-85e5b0ae-255c-4891-a595-0b98c65c9854
      client-secret: test-Hty/M6QshYvPgItX2P0+Kw==

# Configuración de almacenamiento
storage:
  base-path: ${STORAGE_PATH:./files}
  client-separator: true

# Configuración de logging
logging:
  level:
    com.wmc.guiaremision: DEBUG
    org.springframework.security: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

# Configuración del servidor
server:
  port: 8080
  servlet:
    context-path: /
